---
sidebar_position: 6
---

```
const lightCodeTheme = require('prism-react-renderer/themes/github');
const darkCodeTheme = require('prism-react-renderer/themes/dracula');

/** @type {import('@docusaurus/types').Config} */
const config = {
  title: '永c隨筆', // 網站首頁 跟 tag 的 tile
  tagline: '記錄個人學習的成長過程及心得',
  favicon: 'img/favicon.ico', // 更改網站 tag icon

  // 部屬完之前需要來這邊設定 host url
  url: 'https://incomparable-crostata-192195.netlify.app/',

  // Set the /<baseUrl>/ pathname under which your site is served
  // For GitHub pages deployment, it is often '/<projectName>/'
  baseUrl: '/',

  // GitHub pages deployment config.
  // If you aren't using GitHub pages, you don't need these.
  organizationName: 'yongsin0129', // Usually your GitHub org/user name.
  projectName: 'myBlog_docusaurus', // Usually your repo name.

  // 這邊是打包時如果檢查到 link 不存在時，需要提醒還是直接報錯
  onBrokenLinks: 'throw', 
  onBrokenMarkdownLinks: 'warn',

  // Even if you don't use internalization, you can use this field to set useful
  // metadata like html lang. For example, if your site is Chinese, you may want
  // to replace "en" with "zh-Hans or zh-Hant".
  i18n: {
    defaultLocale: 'zh-Hant',
    locales: ['zh-Hant'],
  },




  presets: [
    [
      'classic',
      /** @type {import('@docusaurus/preset-classic').Options} */
      ({
        docs: {
          sidebarPath: require.resolve('./sidebars.js'),
          // 如果想要讓其他人可以發送 pr ，提醒你修改頁面，可以將這個設定打開，並放上自已的 github 
          // editUrl:
          //   'https://github.com/facebook/docusaurus/tree/main/packages/create-docusaurus/templates/shared/',
        },
        blog: {
          showReadingTime: false,
          // 如果想要讓其他人可以發送 pr ，提醒你修改頁面，可以將這個設定打開，並放上自已的 github 
          // editUrl:
          //   'https://github.com/facebook/docusaurus/tree/main/packages/create-docusaurus/templates/shared/',
        },
        theme: {
          customCss: require.resolve('./src/css/custom.css'),
        },
      }),
    ],
  ],



  themeConfig:
    /** @type {import('@docusaurus/preset-classic').ThemeConfig} */
    ({
      // Replace with your project's social card
      image: 'img/docusaurus-social-card.jpg',
      navbar: {
        title: '永c隨筆',
        logo: {
          alt: '永c隨筆 Logo',
          src: 'img/logo.svg',
        },

        // 新增 header 分頁設定
        items: [
          // {
          //   type: 'docSidebar',
          //   sidebarId: 'tutorialSidebar',
          //   label: 'Tutorial',
          //   position: 'left',
          // },
          { to: '/docs/intro', label: 'Docs', position: 'left' },
          { to: '/blog', label: 'Portfolio', position: 'left'},
          {
            href: 'https://github.com/yongsin0129?tab=repositories',
            label: 'GitHub',
            position: 'right',
          },
        ],
      },

      // footer 分頁設定
      footer: {
        style: 'dark',
        links: [
          // {
          //   title: 'Docs',
          //   items: [
          //     {
          //       label: 'Docs',
          //       to: '/docs/intro',
          //     },
          //   ],
          // },
          // {
          //   title: 'Community',
          //   items: [
          //     {
          //       label: 'Stack Overflow',
          //       href: 'https://stackoverflow.com/questions/tagged/docusaurus',
          //     },
          //     {
          //       label: 'Discord',
          //       href: 'https://discordapp.com/invite/docusaurus',
          //     },
          //     {
          //       label: 'Twitter',
          //       href: 'https://twitter.com/docusaurus',
          //     },
          //   ],
          // },
          // {
          //   title: 'More',
          //   items: [
          //     {
          //       label: 'Blog',
          //       to: '/blog',
          //     },
          //     {
          //       label: 'GitHub',
          //       href: 'https://github.com/facebook/docusaurus',
          //     },
          //   ],
          // },
        ],
        copyright: `Copyright © ${new Date().getFullYear()} 永c隨筆, Inc. Built with Docusaurus.`,
      },

      // 這邊可以新增 magic comment 
      // 參考官網 : https://docusaurus.io/docs/markdown-features/code-blocks#custom-magic-comments
      prism: {
        theme: lightCodeTheme,
        darkTheme: darkCodeTheme,
      },

      // 這邊可以自訂網頁預設為黑色or白色，也可以鎖定切換功能
      colorMode: {
        defaultMode: 'dark',
        disableSwitch: false,
        respectPrefersColorScheme: false,
      },

      // 網站最上方的公佈欄
      announcementBar:{
        id: 'announcement id',
        content:'announcement content',
        textColor:'red',
        backgroundColor:'pink',
        isCloseable: true
      }
    }),
};

module.exports = config;
```